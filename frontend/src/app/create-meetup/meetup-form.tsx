'use client'

type Props = {
    // come back to this. not sure right now
}

//do we need to pick something specific? import to come later
const formSchema: MeetupSchema


type FormValues = z.infer<typeof formSchema>

export function MeetupForm(props: Props) {

}